# AGENTS.md

## 프로젝트 개요

이 프로젝트는 Cloudflare에 배포하는 웹 쇼핑몰이다.
데이터는 Cloudflare D1에 저장하고, 프로젝트 파일은 GitHub에 보관한다.

## 가장 중요한 원칙

- 문서에 없다는 것은 자유롭게 결정해도 된다는 뜻이 아니다.
- 문서와 `references/`에서 확인되지 않는 값 · 기능 · 디자인 · 기술 선택은 `UNKNOWN`이다.
- `UNKNOWN`을 그럴듯한 추측이나 일반적인 관례로 채우지 않는다.
- 없는 정보를 있는 척 만들지 않는다.
- 기능보다 장식을 추가하지 않는다.

## 기준으로 삼을 문서

- 기능과 규칙은 `SHOP.md`를 기준으로 한다.
- 화면 디자인은 `DESIGN.md`를 기준으로 한다.
- 상품 이미지와 실제 상품 목록은 `SHOP.md`와 `references/products/`를 함께 기준으로 한다.
- 문서끼리 충돌하면 임의로 판단하지 말고 충돌한 내용을 보고한다.
- 우선순위가 필요한 경우 다음 순서로 판단한다.
  1. `SHOP.md`의 기능 · 데이터 · 범위 규칙
  2. `DESIGN.md`의 화면 디자인 규칙
  3. `references/screens/`의 실제 화면 근거
  4. 이미 구현되어 있는 코드 중 위 문서와 일치하는 부분
- 일반적인 웹 관례나 프레임워크 기본값은 문서의 빈칸을 채우는 근거가 아니다.

### DESIGN.md가 없을 때

- 현재 사용자의 요청에 **`DESIGN.md` 작성 또는 수정이 명시되어 있으면**
  `references/screens/`를 분석해 `DESIGN.md`를 작성하거나 수정한다.
- 현재 요청이 화면 구현인데 `DESIGN.md`가 없다면 임의로 만든 뒤 계속 구현하지 않는다.
  `DESIGN.md`가 없다는 사실을 보고한다.
- 구현 도중 필요한 값이 `DESIGN.md`에 없더라도 `DESIGN.md`를 자동 수정하지 않는다.
- 참고 화면에서 확인되는 값과 확인되지 않는 값을 구분해서 보고한다.
- 사용자가 `DESIGN.md` 수정을 명시적으로 요청했을 때만 문서에 반영한다.

## references 사용법

`references/`는 두 폴더로 나뉘어 있고 쓰는 방법이 다르다.

- `references/screens/` — **최대한 비슷하게 만든다.**
  색, 글꼴의 상대적 크기와 굵기, 여백, 선, 모서리, 정렬, 정보 밀도, 이미지 비율 등
  실제 화면에서 확인되는 디자인 언어를 따른다.
- `references/products/` — **그대로 쓴다.** 실제 상품 이미지다.
  `SHOP.md`의 상품 표가 어느 파일이 어느 상품인지 지정한다.

### Reference Screen Mapping

`DESIGN.md`를 작성할 때 각 참고 화면이 어떤 화면을 위한 것인지 함께 기록한다.

| Reference | Target Screen |
|---|---|
| screen-01 | |
| screen-02 | |
| screen-03 | |

- 실제 파일명은 `references/screens/`에 있는 이름을 사용한다.
- 파일 이름이나 화면 내용만으로 Target Screen을 확정할 수 없으면 임의로 연결하지 않는다.
- 확인할 수 없는 Target Screen은 `UNKNOWN`으로 남긴다.
- 특정 화면에만 있는 디자인은 전체 공통 규칙으로 확대하지 않고
  `Screen-specific Pattern`으로 구분한다.

### 디자인 값의 근거 표시

`DESIGN.md`의 주요 디자인 값은 가능한 경우 값과 근거를 함께 기록한다.

사용할 상태는 다음 네 가지다.

- `CONFIRMED` — `SHOP.md` 또는 사용자의 명시적 지시에서 확정된 값
- `OBSERVED` — 참고 화면에서 직접 확인되는 특성
- `MEASURED` — 참고 화면에서 실제로 측정한 값
- `UNKNOWN` — 근거가 부족해 확정할 수 없는 값

예:

```md
Product image ratio
- Value: 1:1
- Source: SHOP.md
- Status: CONFIRMED

Card border
- Value: 없음
- Source: screen-01
- Status: OBSERVED

Font family
- Value:
- Source:
- Status: UNKNOWN
```

- `UNKNOWN`을 `RECOMMENDED`, `ASSUMED`, `DEFAULT` 같은 이름의 값으로 자동 변환하지 않는다.
- 화면만 보고 정확한 폰트명, HEX 색상, px 값, breakpoint, transition 시간을 추측해 확정하지 않는다.

## 작업 원칙

- 기능을 바꾸기 전에 `SHOP.md`를 확인한다.
- 화면을 바꾸기 전에 `DESIGN.md`를 확인한다.
- `SHOP.md`에 없는 기능은 추가하지 않는다.
- `DESIGN.md`에 없는 색, 글꼴, 모서리 값, 그림자를 새로 만들지 않는다.
- 값이 필요한데 문서에 없으면 참고 화면에서 실제로 확인 가능한지 본다.
- 참고 화면에서도 확인할 수 없으면 `UNKNOWN`으로 두고 임의로 만들지 않는다.
- 단계는 순서대로 진행한다. 현재 단계의 완료 기준을 모두 통과하기 전에 다음 단계 기능을 미리 만들지 않는다.
- 구조 변경이나 여러 파일에 걸친 작업은 먼저 무엇을 바꿀지 짧게 알린다.
- 간단한 수정은 따로 알리지 않고 바로 수행한다.

## 기술 스택 판단

- 기존 프로젝트에 `package.json`, 설정 파일, 소스 구조가 있으면 기존 기술 스택을 따른다.
- 이미 사용 중인 패키지 매니저가 있으면 그대로 사용한다.
- 기존 프로젝트가 비어 있고 기술 스택이 정해져 있지 않다면 React, Next.js, Vue,
  Svelte 등의 프레임워크를 임의로 선택하지 않는다.
- CSS 방식도 기존 프로젝트에 정해진 방식이 있으면 그대로 따른다.
- Tailwind CSS, UI component library, 상태관리 라이브러리, form library 등 새로운
  주요 의존성을 필요하다는 이유만으로 임의로 추가하지 않는다.
- 새로운 프레임워크나 주요 라이브러리가 꼭 필요하다면 무엇이 왜 필요한지 먼저 보고한다.
- Cloudflare D1, GitHub, Cloudflare 배포 외에 문서에 명시되지 않은 서비스나 플랫폼을
  임의로 추가하지 않는다.

## 화면을 만들 때

**1단계 — 말로 먼저.** 무엇을 만들지, 어떤 `SHOP.md` 기능과 `DESIGN.md` 규칙을
적용할지 짧게 밝힌다.

**2단계 — 참고 화면과 맞춰본다.** 만들려는 화면이 참고 화면과 어디가 다른지
확인한다. 차이가 있다면 `DESIGN.md`나 `SHOP.md`의 근거가 있어야 한다.
근거 없는 차이는 만들지 않는다. 그다음 코드를 쓴다.

**참고 화면과 비슷하게 만들 것** — 색, 크기, 여백, 모서리, 선, 그림자를
새로 고르지 않는다. `DESIGN.md`에 있는 값을 우선 사용한다.

정확한 값이 없을 때는 다음 순서로 처리한다.

1. `DESIGN.md`에 값이나 상대적 규칙이 있는지 확인한다.
2. `references/screens/`에서 직접 확인 가능한지 본다.
3. 확인할 수 없으면 `UNKNOWN`으로 둔다.
4. `UNKNOWN`인 값을 구현을 위해 임의 수치로 확정하지 않는다.

**가져오지 않는 것은 두 가지뿐이다.**

1. **그 사이트의 것** — 로고, 브랜드 이름, 그 사이트의 상품 사진과 문구.
2. **우리에게 데이터가 없는 기능** — 필터 사이드바(브랜드·가격대 정보가
   없다), 메가 메뉴(분류가 네 개뿐이다), 프로모션 배너(진행 중인 프로모션이
   없다), 별점·리뷰(리뷰 기능이 없다), 배송·적립 배지(그 기능이 없다).

없는 정보를 있는 척 채우지 않는다. 별점을 지어내거나 "무료배송" 배지를
그냥 붙이지 않는다. 뺀 자리를 장식으로 메우지도 않는다.

### AI스럽게 보이는 임의 디자인 금지

참고 화면이나 `DESIGN.md`에 근거가 없는 다음 요소를 임의로 추가하지 않는다.

- gradient background
- glassmorphism
- glow / blur card
- 과도한 box-shadow
- 모든 영역을 둥근 card로 만드는 방식
- 과도한 border-radius 또는 pill button
- 의미 없는 badge, icon, illustration, decorative shape
- SaaS landing page 형태의 feature card, KPI, testimonial
- 지나치게 큰 Hero heading
- 근거 없는 marketing copy
- 과도한 animation이나 hover effect

실제로 운영되는 쇼핑몰처럼 상품 탐색과 구매 흐름을 우선한다.

## 기능 구현 규칙

- 장바구니, 주문, 인증, 결제의 세부 동작은 `SHOP.md`를 그대로 따른다.
- 같은 상품을 다시 장바구니에 담을 때 중복 행을 만드는 등 `SHOP.md`와 다른 방식으로
  임의 구현하지 않는다.
- 상품 가격과 주문 총액은 브라우저가 보내는 값을 신뢰하지 않는다.
- 주문 금액은 서버에서 저장된 상품 가격과 수량을 바탕으로 다시 계산한다.
- 과거 주문의 가격과 상품명 스냅샷을 현재 상품 정보로 덮어쓰지 않는다.
- 판매 목록에서 빠진 과거 상품을 주문 이력 때문에 바로 삭제하지 않는다.
- 1단계 비회원 장바구니/주문의 식별 방식은 `SHOP.md`에서 `UNKNOWN`으로 남아 있으므로
  임의로 localStorage, cookie, session, 임시 계정 중 하나를 선택하지 않는다.

## 보안

- 시크릿 키나 비밀번호를 코드에 적지 않는다. 환경 변수에 두고 서버에서만 읽는다.
- 사용자가 입력하는 값은 검증한 뒤 처리한다.
- 로그인한 사람은 자신의 정보와 주문만 볼 수 있어야 한다. 주소창의 번호를
  바꿔도 다른 사람의 것에 접근할 수 없어야 한다.
- 비밀번호는 검증된 라이브러리의 해시 기능을 쓰고 직접 구현하지 않는다.
- AI나 결제 서비스 호출은 서버에서만 한다. 브라우저에서 직접 부르지 않는다.
- 결제 완료는 브라우저의 성공 화면만 보고 처리하지 않는다.
- 결제 서비스 결과와 금액을 서버에서 검증한 뒤에만 주문 상태를 변경한다.
- 동일 결제 결과가 반복되어도 중복으로 완료 처리되지 않게 한다.

## 구현 제약

- 상품 이미지는 `references/products/`의 사진만 쓴다. 새로 만들거나 다른
  곳에서 가져오지 않는다.
- **인터넷의 이미지 주소를 코드에 넣지 않는다.** 아이콘, 배경, 대체 이미지도
  마찬가지다. 필요하면 만들지 말고 보고한다.
- 참고 화면에 있는 로고, 브랜드 이름, 그 사이트의 사진과 문구를 쓰지 않는다.
- 글꼴은 `DESIGN.md`가 지정한 것만 쓴다. 다른 글꼴을 임의로 불러오지 않는다.
- `SHOP.md`의 상품 표에 없는 파일을 현재 판매 상품으로 쓰지 않는다. 표가 바뀌면 표를 따른다.
- 사진의 비율이 서로 다르다. 1:1 정사각형 안에 여백을 두고 맞춘다
  (`object-fit: contain`). 잘라내지 않는다.
- 사진은 이미 웹용 크기로 줄여져 있다. 다시 줄이지 않는다.
- 무거운 프레임워크나 UI 라이브러리를 임의로 추가하지 않는다.
- 결제는 직접 구현하지 않고 토스페이먼츠 결제위젯 V2를 연결한다.
- AI 제공자와 모델이 정해지기 전에는 특정 AI SDK를 임의로 설치하거나 연결하지 않는다.
- 핵심 기능을 시각적 장식보다 먼저 완성한다.

## 검증

변경 후 현재 단계에 해당하는 항목을 직접 확인하고 결과를 알린다.

### 공통 검증

- 콘솔 오류가 없다.
- `SHOP.md`에 없는 기능이 추가되지 않았다.
- 화면을 참고 화면과 나란히 놓고 구조, 색, 글꼴의 상대적 크기, 여백이 근거 없이 어긋난 곳이 없다.
- `DESIGN.md`에 없고 참고 화면에서도 확인되지 않는 디자인 값을 임의로 추가하지 않았다.
- `UNKNOWN` 값을 추측으로 채운 곳이 없다.

### 1단계 검증

- 상품 목록이 `SHOP.md`의 상품 표와 일치한다.
- 분류 필터, 상품 상세, 장바구니, 주문이 동작한다.
- 수량이 1~99 범위를 벗어나지 않는다.
- 같은 상품을 다시 담으면 수량이 합쳐진다.
- 수량을 바꾸면 합계 금액이 맞게 바뀐다.
- 서버에서 계산한 주문 총액과 저장된 주문 총액이 일치한다.
- 주문 후 장바구니가 비워진다.

### 2단계 검증

- 회원가입, 로그인, 로그아웃이 동작한다.
- 이메일 중복 가입이 차단된다.
- 비밀번호 원문을 저장하지 않는다.
- 로그인하지 않은 사용자가 장바구니와 주문에 접근할 수 없다.
- 다른 사용자의 정보나 주문에 접근할 수 없다.

### 3단계 검증

- 토스페이먼츠 문서용 테스트 키로 테스트 결제 흐름을 확인한다.
- 서버 검증 전에는 주문을 `paid`로 바꾸지 않는다.
- 주문 금액과 결제 금액 불일치가 완료 처리되지 않는다.
- 중복 결제 결과가 중복 완료 처리되지 않는다.
- 실패 또는 취소 시 `paid`가 되지 않는다.

### 4단계 검증

- `English` 버튼, 로딩 상태, 성공 결과, 실패 안내가 동작한다.
- 생성된 상품 소개가 세 문장 이내다.
- 실패해도 기존 상품 화면이 유지된다.
- 지정되지 않은 AI 서비스나 모델을 임의로 추가하지 않았다.

### 배포 후 검증

- 배포한 뒤에는 공개 주소에서 현재 단계의 핵심 흐름을 다시 확인한다.

## 문서를 고칠 때

아래 파일은 **내가 고쳐달라고 명시적으로 요청할 때만** 바꾼다.

- `AGENTS.md` — 이 파일
- `SHOP.md`
- `DESIGN.md`
- `references/` 안의 파일

예외는 하나뿐이다.

- 현재 요청 자체가 `DESIGN.md 작성`을 명시하고 있다면 그 요청 범위 안에서 새로 작성할 수 있다.

구현하는 도중에 이 파일들을 필요에 따라 바꾸지 않는다. 코드가 문서와 맞지 않으면
문서를 고치지 말고 **코드를 문서에 맞춘다.** 문서 쪽이 틀렸거나 서로 충돌한다고 판단되면
직접 고치지 말고 무엇이 왜 어긋났는지 알린다.

고쳐달라는 요청을 받으면 이렇게 한다.

1. 무엇을 어떻게 바꿀지 먼저 보여주고 승인을 받는다.
2. 요청받은 파일만 바꾼다. 다른 문서는 건드리지 않는다.
3. 문서를 바꾼 뒤에 코드도 함께 맞춰야 하면 그 사실을 알린다.
