{
  "name": "shop",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.client.json",
    "check": "tsc -p tsconfig.worker.json --noEmit && tsc -p tsconfig.client.json --noEmit",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260829.1",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3"
  }
}
